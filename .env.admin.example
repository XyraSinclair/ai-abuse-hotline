# AI Abuse Hotline Admin Configuration
# Copy this file to .env.admin and fill in your values
# This file should NOT be committed to version control

# SSH connection to your server (required)
HOTLINE_SERVER=user@your-server-ip

# Path to SSH key (optional, defaults to ~/.ssh/ai_abuse)
# HOTLINE_SSH_KEY=~/.ssh/ai_abuse

# Database path on server (optional, defaults to /opt/aiabusehotline/data/hotline.db)
# HOTLINE_DB_PATH=/opt/aiabusehotline/data/hotline.db
