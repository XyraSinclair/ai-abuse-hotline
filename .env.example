# AI Abuse Hotline - Environment Variables
# Copy this to .env and fill in your values

# =============================================================================
# REQUIRED
# =============================================================================

# Admin API authentication token (generate a secure random string)
ADMIN_TOKEN=

# =============================================================================
# OPTIONAL
# =============================================================================

# Environment (development or production)
ENV=production

# Database path (defaults to ./data/hotline.db)
DB_PATH=/opt/aiabusehotline/data/hotline.db

# OpenRouter API key for LLM-based spam filtering
# If not set, spam filtering is disabled (reports still accepted)
OPENROUTER_API_KEY=

# Node gateway settings
NODE_ENV=production
PYTHON_INTERNAL_URL=http://127.0.0.1:8000
